pip list
pip list --not-required
pip index versions numpy    - verification for numpy version

python -m venv xgboost_env - create new environment

pip install -r requirements.txt

1. cd\ env full path
2. .\scripts\activate

How to know what current environment is activated:
python -c "import sys; print('Python executable:', sys.executable); print('Environment path:', sys.prefix)"